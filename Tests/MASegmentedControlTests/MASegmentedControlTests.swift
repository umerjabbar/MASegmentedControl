import XCTest
@testable import MASegmentedControl

final class MASegmentedControlTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MASegmentedControl().text, "Hello, World!")
    }
}
